package ar.edu.unlam.mobile.scaffold.data.Sw.network

import android.telecom.Call
import retrofit2.Callback
import retrofit2.Response

class Planeta2 {
    //esto se llama de
/*
    fun getPlanet(planetId: Int): Any {
        val swapiRepository = SwApiProviderpiRepository(swapiService) // Puedes pasar el servicio como argumento
        val planetId = 1 // El ID del planeta que deseas consultar

        val callback = object : Callback<Planeta> {
            override fun onResponse(call: Call<Planeta>, response: Response<Planeta>) {
                if (response.isSuccessful) {
                    val planet = response.body()
                    // Maneja los datos del planeta en la interfaz de usuario
                } else {
                    // Maneja el error en caso de una respuesta no exitosa
                }
            }

            override fun onFailure(call: Call<Planeta>, t: Throwable) {
                // Maneja el error en caso de fallo en la solicitud
            }
        }

        swapiRepository.getPlanet(planetId, callback)
    }*/

}
